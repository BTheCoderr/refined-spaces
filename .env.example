# Server-side only (Netlify environment variables in production).
# Copy to .env for local `netlify dev` — never commit .env.
RESEND_API_KEY=
RESEND_TO_EMAIL=werefinespaces@gmail.com
RESEND_FROM_EMAIL="Refined Spaces <onboarding@resend.dev>"
