export function SectionEyebrow({ children }) {
  return (
    <span className="inline-block font-accent italic text-gold text-base md:text-lg tracking-[0.15em] mb-5">
      {children}
    </span>
  )
}
